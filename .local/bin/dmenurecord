#!/bin/sh
# dmenurecord-helper — zeigt Hinweistext im Terminal

TERM="${TERMINAL:-st}"

# neues Terminal öffnen mit Hinweistext
exec "$TERM" -e sh -c '
  clear
  cat <<EOF
──────────────────────────────
 dmenurecord – Hinweis
──────────────────────────────

Du nutzt gerade nicht die passende Variante.

So stellst du das um:

  cp ~/.local/src/dotfiles/.local/bin/dmenurecord-intel \
     ~/.local/src/dotfiles/.local/bin/dmenurecord
  chmod +x ~/.local/src/dotfiles/.local/bin/dmenurecord

oder für NVIDIA:

  cp ~/.local/src/dotfiles/.local/bin/dmenurecord-nvidia \
     ~/.local/src/dotfiles/.local/bin/dmenurecord
  chmod +x ~/.local/src/dotfiles/.local/bin/dmenurecord

(Später auch für AMD: dmenurecord-amd)

Danach funktioniert dein Keybinding wieder mit
der gewählten Variante.
──────────────────────────────

Drücke ENTER um das Terminal zu schließen…
EOF
  read _
'
