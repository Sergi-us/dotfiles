#!/bin/sh
## 2025-10-10   SARBS
# Abhängigkeiten: lyrics-fetch-batch, mpc
# ~/.local/bin/lyrics-fetch-queue
# Lädt Lyrics für alle Songs in der Queue
# ist in dem Musikplayer 'rmpc' eingebunden

export SELECTED_SONGS=$(mpc playlist -f '%file%')
lyrics-fetch-batch
