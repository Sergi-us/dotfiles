# Diese Dateien werden automatisch aktualisiert (wenn bearbeitet/gespeichert in Vim)

# keys	filename										# Beschreibung
bf	${XDG_CONFIG_HOME:-$HOME/.config}/shell/bm-files	# Diese Datei
bd	${XDG_CONFIG_HOME:-$HOME/.config}/shell/bm-dirs		# Eine Liste Verzeichnissen "Favoriten" weden von lf und nvim
cfx	${XDG_CONFIG_HOME:-$HOME/.config}/x11/xresources	# Farben, Themes und Variablen für X11
cfs	~/.local/share/sarbs/snippets						# wird für Snippets genutzt

# These do not update automatically, but on the next new instance of a program:

cfv	${XDG_CONFIG_HOME:-$HOME/.config}/nvim/init.lua				# vim/neovim config
cfz	$ZDOTDIR/.zshrc												# zsh (shell) config
cfa	${XDG_CONFIG_HOME:-$HOME/.config}/shell/aliasrc				# aliases used by zsh (and potentially other shells)
cfp	${XDG_CONFIG_HOME:-$HOME/.config}/shell/profile				# profile file for login settings for zsh
cfm	${XDG_CONFIG_HOME:-$HOME/.config}/mutt/muttrc				# mutt (email client) config
cfn	${XDG_CONFIG_HOME:-$HOME/.config}/newsraft/config			# newsboat (RSS reader)
cfu	${XDG_CONFIG_HOME:-$HOME/.config}/newsraft/feeds			# RSS urls for newsboat
cfl	${XDG_CONFIG_HOME:-$HOME/.config}/lf/lfrc					# lf (file browser) config
cfL	${XDG_CONFIG_HOME:-$HOME/.config}/lf/scope					# lf's scope/preview file
cfX	${XDG_CONFIG_HOME:-$HOME/.config}/nsxiv/exec/key-handler	# nsxiv (image viewer) key/script handler
